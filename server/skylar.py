'''an algo to search through interactions for similar user input similar denoting a high probability of same topic
    if similar is found we use the similarity to determine /main theme/ of the conversation and use that to determine the prompt
    if no similar is found we craft a completely new prompt
    we then use the prompt to generate a response
it would have two parts 
Similarity Detection: Search through past interactions for input that is similar to the current user input.
Topic Extraction: If similar past interactions are found, use these interactions to determine the main theme of the conversation.
'''



